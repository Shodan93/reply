Square icon-only button for toolbars, nav utilities, and card actions.

```jsx
<IconButton icon="magnifying-glass" aria-label="Suche" />
```

Variants: `ghost` (default, transparent), `outline` (bordered), `inverse` (ink-filled, for dark sections). Always pass `aria-label`.
